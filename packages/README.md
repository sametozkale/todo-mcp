# Workspace packages (`@yalp/*`)

| Package | Purpose |
|--------|---------|
| `@yalp/types` | Shared TypeScript interfaces (todos, lists, API shapes). |
| `@yalp/supabase` | Supabase client helpers (browser / service role). |
| `@yalp/utils` | Placeholder (`export {}`); reserved for shared utilities. |
